# Example environment
PORT=5177
NODE_ENV=development
SESSION_NAME=av.sid
SESSION_IDLE_MINUTES=30
SESSION_ABSOLUTE_HOURS=8
CSRF_COOKIE_NAME=XSRF-TOKEN
CORS_ORIGIN=http://localhost:5173
# Leave SESSION_SECRET empty; the server will generate and persist to ./data/.session-secret automatically.
